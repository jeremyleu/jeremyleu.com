import React from 'react';

import { Section, SectionContainer } from './Page.styles';

export const Page = () => {
  return (
    <SectionContainer>
      <Section />
    </SectionContainer>
  );
};

export default Page;
